name: Testing
on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'

jobs:
  Hello_World:
    runs-on: ubuntu-latest
    steps:
      - name: Hello step
        run: echo "Hello World!"